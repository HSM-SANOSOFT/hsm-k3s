apiVersion: v1
kind: Service
metadata:
  name: chartdb
  namespace: app
  labels:
    app: chartdb
spec:
  type: ClusterIP
  selector:
    app: chartdb
  ports:
    - name: tcp-8080
      port: 8080
      targetPort: 80
      protocol: TCP
