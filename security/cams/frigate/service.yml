apiVersion: v1
kind: Service
metadata:
  name: hsm-security-cams-frigate
  namespace: security
spec:
  type: ClusterIP
  selector:
    app: hsm-security-cams-frigate
  ports:
    - name: http
      port: 80
      targetPort: 8971
      protocol: TCP